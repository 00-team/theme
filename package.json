{
    "name": "theme",
    "scripts": {
        "vscode:package": "cd vscode && vsce package",
        "vscode:publish": "cd vscode && vsce publish"
    },
    "dependencies": {
        "vsce": "^2.7.0"
    }
}
